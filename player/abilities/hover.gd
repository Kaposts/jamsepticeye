extends Ability

# func apply(player, delta):
# 	if not player.is_on_floor() and Input.is_action_pressed("ui_accept"):
# 		player.velocity.y += player.gravity * (player.hover_gravity_scale - 1.0) * delta  # reduce gravity